FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

ARG REACT_APP_API_URL
ENV REACT_APP_API_URL=$REACT_APP_API_URL
ENV HOST=0.0.0.0
ENV PORT=3001
ENV DANGEROUSLY_DISABLE_HOST_CHECK=true

EXPOSE 3001

CMD ["npm", "run", "start"]
